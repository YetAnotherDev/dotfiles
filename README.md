# Dot Files

I manage these dotfiles using gnu stow. To do this you just navigate to the root dotfiles repo directory and run the stow commmand on the top level dirctory that you would like to symlink. for
example: if i wanted to install my config for aerospace then i would just run the 'stow aerospace' command and it would link those configs in the correct location.
